# Dependencias de Python para Mario AI Pipeline y MarioGPT
kaitaistruct
datasets
huggingface_hub
torch
transformers
scipy
pillow
accelerate

# The browser proof uses JDK 17+ and Maven 3.9+.
# They are system tools, not Python packages, so they are documented here
# instead of being passed to pip.
